<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_appraisal_template_list" model="ir.ui.view">
        <field name="name">appraisal.template.list</field>
        <field name="model">appraisal.template</field>
        <field name="arch" type="xml">
            <list string="Appraisal Templates">
                <field name="name" />
                <field name="evaluation_group_id" />
                <field name="competency_template_id" string="Competency Template" />
                <field name="total_kpi_score" />
            </list>
        </field>
    </record>

    <record id="view_appraisal_template_form" model="ir.ui.view">
        <field name="name">appraisal.template.form</field>
        <field name="model">appraisal.template</field>
        <field name="arch" type="xml">
            <form string="Appraisal Template">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button"
                            icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                options="{'terminology': 'archive'}" />
                        </button>

                        <!-- Smart button: jump into linked competency template -->
                        <button
                            name="action_open_competency_template"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-star"
                            invisible="not competency_template_id">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value">
                                    <field name="competency_group_count" />
                                </span>
                                <span class="o_stat_text">Competency Groups</span>
                            </div>
                        </button>
                    </div>

                    <group>
                        <group>
                            <field name="name" placeholder="e.g., Druksmart Template" />
                            <field name="evaluation_group_id" options="{'no_create': True}" />
                        </group>
                        <group>
                            <field name="competency_template_id"
                                string="Competency Template"
                                options="{'no_create': False, 'no_open': False}" />
                            <field name="total_kpi_score" string="Total KPI Score" readonly="1" />
                        </group>
                    </group>

                    <!-- Warning when no competency template linked -->
                    <div class="alert alert-warning mt-2 mb-0" role="alert"
                        invisible="competency_template_id">
                        <i class="fa fa-exclamation-triangle me-2" title="Warning" /> No competency
                        template linked yet. Select one from the Competency Template field above. </div>

                    <field name="id" invisible="1" />

                    <!-- KRA Tabs Widget -->
                    <field name="kra_ids" widget="kra_tabs_widget">
                        <list>
                            <field name="name" />
                            <field name="sequence" />
                            <field name="kpi_ids">
                                <list>
                                    <field name="name" />
                                    <field name="description" />
                                    <field name="criteria" />
                                    <field name="score" />
                                </list>
                            </field>
                        </list>
                    </field>
                </sheet>
            </form>
        </field>
    </record>

    <!-- KRA Form View — unchanged -->
    <record id="view_appraisal_kra_form" model="ir.ui.view">
        <field name="name">appraisal.kra.form</field>
        <field name="model">appraisal.kra</field>
        <field name="arch" type="xml">
            <form string="Key Result Area">
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="template_id" readonly="1" />
                        <field name="sequence" />
                    </group>

                    <group string="Key Performance Indicators">
                        <field name="kpi_ids" nolabel="1">
                            <list editable="bottom">
                                <field name="name" />
                                <field name="description" />
                                <field name="score" />
                                <field name="criteria" />
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_appraisal_template" model="ir.actions.act_window">
        <field name="name">Templates</field>
        <field name="res_model">appraisal.template</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first appraisal template
            </p>
        </field>
    </record>

</odoo>